#!/usr/bin/env ruby

require_relative './../lib/linguist-ext'

def linguist_ext(args)
    include Linguist
    path = Dir.pwd
    dirpath = args[0]
    blobs = index_dir(dirpath)
    s = merge_blobs(blobs)
    s.present
end

linguist_ext(ARGV)