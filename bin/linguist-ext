#!/usr/bin/env ruby

require_relative './../lib/linguist-ext'

def linguist_ext(args)
    include Linguist
    path = Dir.pwd
    dirpath = args[0]
    blobs = index_dir(dirpath)
    puts blobs[0]
end

linguist_ext(ARGV)